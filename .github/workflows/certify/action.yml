name: Apply Audits
description: 'Call cargo-vet certify for all the given audits'
inputs:
  GITHUB_TOKEN:
    description: 'GitHub token'
    required: true
  audits:
    description: 'audits to certify'
    required: true
runs:
  using: 'node16'
  main: 'certify.js'
